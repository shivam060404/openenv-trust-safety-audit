# COLISEUM — Python Dependencies
# Install: pip install -r requirements.txt
#
# openenv-core==0.2.3 pulls fastmcp 3.x which controls: uvicorn, pydantic,
# python-dotenv, openai, fastapi, huggingface_hub, numpy, pandas, tqdm, etc.
# Those are NOT pinned here — openenv-core's resolver owns them.
# We only pin the pure ML stack that openenv-core doesn't touch.
#
# Mac Apple Silicon (MPS): works as-is.
# Kaggle/CUDA: install unsloth + bitsandbytes separately.
# Optional: pip install mergekit | wandb

# ── OpenEnv (owns its own transitive deps — don't override them) ─────────────
openenv-core==0.2.3

# ── Core ML — pinned (no overlap with openenv-core's dep tree) ────────────────
torch==2.5.1
transformers==4.47.1
accelerate==1.2.1
peft==0.14.0
trl==0.15.2
datasets==3.1.0
safetensors==0.4.5
sentencepiece==0.2.0
tokenizers==0.21.0

# ── Data & Eval ───────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0

# ── Single known hard conflict: torch + datasets both pull fsspec ─────────────
# openenv-core resolves fsspec to 2026.3.0; torch 2.5.1 needs >=2023.1.0 — compatible.
# No pin needed. Leaving this comment so the decision is documented.


openenv