# COLISEUM Defender — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.1.0
transformers>=4.43.0
accelerate>=0.28.0
bitsandbytes>=0.42.0
peft>=0.10.0
trl==0.15.2
datasets>=2.18.0
huggingface_hub>=0.22.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# ── Unsloth (install separately — see scripts/kaggle_setup.sh) ───────────────
# unsloth  # handled specially on Kaggle

# ── Inference API ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
requests>=2.31.0

# ── Data & Eval ───────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.65.0

# ── Logging ───────────────────────────────────────────────────────────────────
wandb>=0.17.0
